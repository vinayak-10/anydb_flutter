# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/
Patients*

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# OAuth Secrets
secrets.json

# Keystores
*.jks
*.keystore
*.p12
*.pfx
key.properties

# Local cross-compilation caches and build outputs
.xwin-cache/
build_win/
.antigravitycli/

# Maya local dependencies and MSVC SDK (excluding forced-preinclude header)
xyz.maya/*
!xyz.maya/preinclude.h

# Firebase CLI Local operational state
.firebase/

# Local database imports and exports (keeping schema/configs safe)
Patients_*.json
*Physio*.json

# Storage self-healing corruption backups
*.corrupted_*
*.json.corrupted_*

# Excel workbook reports
*.xlsx

# Scratch/temp test files
test_sign_in.dart

# Generated visual previews (Primary vector SVGs remain tracked)
assets/*.jpg

# iOS packaging configuration template
ios/ExportOptions.plist
android/app/anydb-release-build-33.apk
android/app/keystore2john.py
android/app/keystore_base64.txt
android/app/keystore_base64_old.txt
android/app/keystore_hash.txt
android/app/pfx2john.py
android/app/pkcs12_hash.txt

# Firebase project config (contains project ID)
.firebaserc

# Firebase google-services — contains live API keys and OAuth client IDs
android/app/google-services.json

# Android build reports — generated artifacts, not source
android/build/reports/
